package com.acme.testcode;

import org.apache.commons.collections4.CollectionUtils;

public final class Test {

  private boolean isEmpty1 = CollectionUtils.isEmpty(new ArrayList<>());
  private boolean isEmpty2 = OtherType.OTHER_COLLECTION == null || OtherType.OTHER_COLLECTION.isEmpty();
  private List<String> strCollection = null;

  void withVar(Collection<?> collection) {
    var empty = collection == null || collection.isEmpty();
  }

  boolean inReturn() {
    return strCollection == null || strCollection.isEmpty();
  }

  boolean inReturnWithMethod() {
    return CollectionUtils.isEmpty(call());
  }
}
